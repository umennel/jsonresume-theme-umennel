{{#resume.basics}}
	<div id="photo-header">
		<div id="photo">
			<img src="{{picture}}" alt="avatar">
		</div>
	</div>
	<div id="text-header">
		
		<h1>{{name}}<br>{{#if label}}<span>{{label}}</span>{{/if}}</h1>
	</div>
{{/resume.basics}}